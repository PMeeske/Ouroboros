name: Copilot Agent Solver

on:
  issues:
    types:
      # Trigger when an issue is opened or labeled
      - opened
      - labeled

permissions:
  # Required to create PRs
  pull-requests: write
  # Required to read/write repo contents and issues
  contents: write
  issues: write

jobs:
  dispatch-copilot-agent:
    # Run if the issue has the 'copilot-agent' label
    if: contains(github.event.issue.labels.*.name, 'copilot-agent')
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Authenticate, Install, and Run Copilot
        id: copilot-suggest
        shell: bash
        env:
          GH_TOKEN: ${{ secrets.COPILOT_BOT_TOKEN }}
          GH_ISSUE_NUMBER: ${{ github.event.issue.number }}
          GH_ISSUE_TITLE: ${{ github.event.issue.title }}
        run: |
          # Step 1: Perform the non-interactive login.
          # We unset GH_TOKEN right before the command, but pipe the same token value in.
          # This satisfies the CLI's requirement that GH_TOKEN not be set in the environment during `auth login`.
          (unset GH_TOKEN; echo "${GH_TOKEN}" | gh auth login --with-token)
          echo "✅ gh auth login completed."

          # Step 2: Install the Copilot extension.
          # The GH_TOKEN env var is still available for the rest of the script.
          gh extension install github/gh-copilot
          echo "✅ gh-copilot extension installed."

          # Step 3: Run the suggest command.
          echo "🤖 Asking Copilot to suggest a fix..."
          gh copilot suggest "Fix issue #${GH_ISSUE_NUMBER}: ${GH_ISSUE_TITLE}" --shell-out bash > copilot_plan.sh
          
          echo "🤖 Copilot has generated a plan:"
          cat copilot_plan.sh
          
          # Make the plan executable
          chmod +x copilot_plan.sh
          
          # Run the plan generated by Copilot
          ./copilot_plan.sh
          echo "✅ Copilot plan executed."

      - name: Create Pull Request
        shell: bash
        env:
          GH_TOKEN: ${{ secrets.COPILOT_BOT_TOKEN }}
          GH_ISSUE_NUMBER: ${{ github.event.issue.number }}
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
        
          if ! git diff --quiet HEAD; then
            echo "✅ Changes found. Creating Pull Request..."
            gh pr create --title "🤖 Copilot fix for #${GH_ISSUE_NUMBER}" --body "Addresses issue #${GH_ISSUE_NUMBER} with a solution suggested by \`gh copilot\`." --fill
          else
            echo "⚠️ No changes were made by Copilot. Skipping PR creation."
          fi
